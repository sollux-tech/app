export interface KpiSmartFrequency {
  id: string;
  user_id: string;
  code: number;
  description: string;
  created_at: string;
}

export interface KpiSmartFrequencyFormData {
  description: string;
}